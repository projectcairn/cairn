#!/bin/bash
# WiFi hotspot setup — coming soon
#
# This script will configure hostapd and dnsmasq for a Cairn WiFi hotspot.
# For now, configure manually:
#   1. Edit /etc/hostapd/hostapd.conf
#   2. Edit /etc/dnsmasq.conf
#   3. systemctl enable --now hostapd dnsmasq

echo "WiFi hotspot setup — coming soon"
echo "See /etc/hostapd/hostapd.conf for manual configuration."
exit 0
